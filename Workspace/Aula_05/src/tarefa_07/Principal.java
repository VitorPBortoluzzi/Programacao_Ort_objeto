package tarefa_07;

public class Principal {
	public static void main(String[] args) {
//		TODO 7 – Pesquise quais são as Exceções existentes que já possuem tratamento e explique brevemente como cada uma funciona. Teste duas exceções.
	//		Em linguagens orientadas a objetos como Java, as exceções são eventos que interrompem o fluxo normal de um programa. Quando dizemos que "já possuem tratamento", 
//	//		refere-se a exceções pré-definidas na API da linguagem que podem ser capturadas e tratadas por blocos try-catch para evitar o encerramento abrupto da aplicação
//		 EXMP:
		//	1-	NullPointerException (NPE): Ocorre quando o código tenta usar uma referência de objeto que está nula (não instanciada).
		//	2-	ArrayIndexOutOfBoundsException: Disparada quando tenta acessar um índice de um array que é inválido (negativo ou maior que o tamanho do array).
	}
}
